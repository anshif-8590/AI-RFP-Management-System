
#  Backend Environment #

MONGODB_URI=your-mongodb-connection-url
PORT=5000

# OpenAI API (for RFP creation, proposal parsing & comparison)
OPENAI_API_KEY=your-openai-api-key
OPENAI_MODEL=gpt-4o-mini   # or the model you use

# Mailgun (email sending + inbound webhook)
MAILGUN_API_KEY=your-mailgun-api-key
MAILGUN_DOMAIN=your-mailgun-domain
MAILGUN_FROM_EMAIL=your-sender-email@example.com
MAILGUN_WEBHOOK_SIGNING_KEY=your-mailgun-webhook-signing-key   # optional but recommended

# URL used in Mailgun webhook to send vendor replies to your server
BACKEND_BASE_URL=http://localhost:5000



#  Frontend Environment 


# API base URL for connecting frontend to backend
REACT_APP_BACKEND_URL=http://localhost:5000
